pluginManagement { resolutionStrategy {
  eachPlugin {
    if (requested.id.id == "kotlin") useModule("org.jetbrains.kotlin:kotlin-gradle-plugin:${requested.version}")
  }
} }
rootProject.name = "kamet-dse"
